/**
 * GENERIC DATA TABLE
 *
 * Componente de tabla genérico y reutilizable para mostrar cualquier tipo de entidad.
 * Basado en TanStack Table (React Table v8).
 *
 * Características:
 * - Paginación
 * - Ordenamiento
 * - Filtros
 * - Selección de filas
 * - Acciones por fila (editar, eliminar)
 * - Responsive
 *
 * Uso:
 * <GenericDataTable
 *   data={users}
 *   columns={userColumns}
 *   onEdit={handleEdit}
 *   onDelete={handleDelete}
 * />
 */

'use client'

export function GenericDataTable() {
  return (
    <div>
      {/* TODO: Implementar tabla genérica con TanStack Table */}
      <p>Generic Data Table Component</p>
    </div>
  )
}
