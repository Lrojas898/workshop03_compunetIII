/**
 * DATA TABLE PAGINATION CONTROLS
 *
 * Componente de controles de paginación para la tabla genérica.
 *
 * Características:
 * - Botones de navegación (primera, anterior, siguiente, última página)
 * - Selector de items por página
 * - Información de página actual y total
 */

'use client'

export function DataTablePaginationControls() {
  return (
    <div>
      {/* TODO: Implementar controles de paginación */}
      <p>Pagination Controls</p>
    </div>
  )
}
