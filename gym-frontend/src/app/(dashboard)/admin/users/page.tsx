/**
 * USERS MANAGEMENT PAGE
 *
 * Página de gestión de usuarios del sistema.
 * Permite operaciones CRUD sobre usuarios.
 *
 * Características:
 * - Listado de usuarios con DataTable
 * - Filtros por rol, estado, fecha de registro
 * - Crear, editar, eliminar usuarios
 * - Asignación de roles
 * - Solo accesible para rol ADMIN
 */

export default function UsersManagementPage() {
  return (
    <div>
      <h1>Users Management</h1>
      {/* TODO: Usar EntityManager component con configuración de usuarios */}
    </div>
  )
}
