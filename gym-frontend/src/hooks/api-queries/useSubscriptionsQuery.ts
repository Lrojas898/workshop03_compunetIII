/**
 * SUBSCRIPTIONS HOOKS
 *
 * React Query hooks para gestión de suscripciones.
 *
 * Hooks:
 * - useSubscriptions: Obtener lista de suscripciones
 * - useSubscription: Obtener una suscripción por ID
 * - useUserSubscription: Obtener suscripción activa de un usuario
 * - useCreateSubscription: Crear nueva suscripción
 * - useUpdateSubscription: Actualizar suscripción
 * - useCancelSubscription: Cancelar suscripción
 * - useAddMembershipToSubscription: Agregar membresía a suscripción
 */

'use client'

// TODO: Implementar hooks de suscripciones con React Query

export const useSubscriptions = () => ({})
export const useSubscription = (id: string) => ({})
export const useUserSubscription = (userId: string) => ({})
export const useCreateSubscription = () => ({})
export const useUpdateSubscription = () => ({})
export const useCancelSubscription = () => ({})
export const useAddMembershipToSubscription = () => ({})
