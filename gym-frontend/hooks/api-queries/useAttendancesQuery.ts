/**
 * ATTENDANCES HOOKS
 *
 * React Query hooks para gestión de asistencias.
 *
 * Hooks:
 * - useAttendances: Obtener lista de asistencias
 * - useAttendance: Obtener una asistencia por ID
 * - useUserAttendances: Obtener asistencias de un usuario
 * - useActiveAttendances: Obtener asistencias activas (usuarios en el gym)
 * - useCheckIn: Registrar check-in
 * - useCheckOut: Registrar check-out
 */

'use client'

// TODO: Implementar hooks de asistencias con React Query

export const useAttendances = () => ({})
export const useAttendance = (id: string) => ({})
export const useUserAttendances = (userId: string) => ({})
export const useActiveAttendances = () => ({})
export const useCheckIn = () => ({})
export const useCheckOut = () => ({})
